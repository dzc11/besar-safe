# BesarSafe

Crowdsourced flood-reporting geo-mapping app for Kelurahan Besar.

## Quickstart (dev)

1. Copy `.env.example` to `.env` and adjust if needed.
2. Start Postgres + app locally with Docker:
   ```bash
   docker-compose up --build
